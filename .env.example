# Copy to .env (or export in your shell) and fill in. Never commit real keys.
# Only needed for the API-model steering experiments (Section 3.2) and for
# gated Hugging Face model downloads.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
HF_TOKEN=
